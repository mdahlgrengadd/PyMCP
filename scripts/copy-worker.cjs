#!/usr/bin/env node

/**
 * PyMCP worker copy script (deprecated)
 *
 * Note: The PyMCP worker is now compiled as part of desktop-host
 * This script is kept for backwards compatibility but does nothing
 */

console.log(`ℹ️  Worker compilation handled by desktop-host rspack config`);
console.log(`   No copy needed.`);
